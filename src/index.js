import React from "react";
import ReactDOM from "react-dom";
import AnimatedPies from "./AnimatedPies";

const rootElement = document.getElementById("root");
ReactDOM.render(<AnimatedPies />, rootElement);
